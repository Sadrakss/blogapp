<div class="jumbotron">
    <h1 class="display-4">Welcome to the node blog</h1>
    <p class="lead">This is a simple blog, develop with nodejs.</p>
    <hr class="my-4">
    <p>Here you will be able to read about the world of programming.</p>
    <a class="btn btn-primary btn-lg" href="#" role="button">Create an account</a>
</div>
<hr>
<h2>recent posts: </h2>

{{#each posts}}
<div class="card mt-4">
    <div class="card-body">
        <h3>{{title}}</h3>
        <p>{{description}}</p>
        <a href="/post/{{slug}}"><button class="btn btn-success">Read more</button></a>
        <hr>
        <small>Category: {{category.name}}</small><br>
        <small>Publication date: {{date}}</small>
    </div>
</div>
{{else}}
<h5>No Posts..</h5>
{{/each}}